# MicroBioLab 构建方案

## 1. 目标与产物

当前版本：`v1.1.1`

构建产生两个自有 OCI 镜像：

```text
ghcr.io/codeaix/microbio-lab-app:v1.1.1
ghcr.io/codeaix/microbio-lab-builder:v1.1.1
```

- `app`：Fastify API、数据库迁移、管理员 CLI、React 管理端和学生端；
- `builder`：独立 JSX 验证/构建 Worker。

生产 Compose 还使用：

```text
postgres:17-alpine
nginx:1.27-alpine
```

正式 GHCR 镜像同时发布 `linux/amd64` 和 `linux/arm64`。离线 Docker archive 必须按目标 VPS 架构分别制作。

## 2. 源码与镜像对应关系

每个正式镜像必须从同名 Git tag 构建。例如 `v1.1.1` 镜像只能来自 `v1.1.1` tag，不得从后续 `main` 重新构建后覆盖该标签。

关键输入：

```text
infra/docker/Dockerfile.app
infra/docker/Dockerfile.builder
package-lock.json
apps/api
apps/web
services/builder
packages/shared
db/migrations
```

源码归档应使用：

```bash
git archive --format=tar.gz \
  --prefix=MicroBioLab-v1.1.1/ \
  -o MicroBioLab-v1.1.1-source.tar.gz \
  v1.1.1
```

`git archive` 不包含 `.env`、Git 历史、`node_modules`、构建缓存或未跟踪的用户实验文件。

## 3. 构建前检查

```bash
node --version
npm --version
docker --version
docker compose version
docker buildx version
git status --short
git describe --tags --exact-match
```

要求：

- Node.js 24+；
- npm 使用仓库 `package-lock.json`；
- Docker Engine 24+；
- Docker Buildx；
- 构建正式版本时工作树必须干净，并且 HEAD 正好位于目标 tag；
- 不把 `.env` 或用户实验文件加入构建上下文归档。

## 4. 代码质量与应用构建

```bash
npm ci
npm audit --audit-level=high
npm run lint
npm run typecheck
npm test
npm run build
bash -n scripts/*.sh tests/integration/*.sh
git diff --check
```

构建目录：

```text
packages/shared/dist
apps/web/dist
apps/api/dist
services/builder/dist
```

这些本地产物用于验证，不需要提交 Git。

## 5. 单架构 Docker 构建

在 x86_64 VPS 或 CI 上构建 `linux/amd64` 并载入本地 Docker：

```bash
VERSION="v1.1.1"

docker buildx build \
  --platform linux/amd64 \
  --file infra/docker/Dockerfile.app \
  --tag "ghcr.io/codeaix/microbio-lab-app:${VERSION}" \
  --load .

docker buildx build \
  --platform linux/amd64 \
  --file infra/docker/Dockerfile.builder \
  --tag "ghcr.io/codeaix/microbio-lab-builder:${VERSION}" \
  --load .
```

ARM64 将 `linux/amd64` 改为 `linux/arm64`。跨架构 `--load` 仅用于保存/分发，不能保证可在当前 CPU 上运行。

## 6. Docker 集成闭环

开发 Compose 使用本地源码构建，不依赖正式 GHCR 标签：

```bash
cp .env.example .env
docker compose -f compose.dev.yaml up -d --build --wait
./tests/integration/sample-flow.sh
npm run test:e2e
docker compose -f compose.dev.yaml restart app builder
curl --retry 20 --retry-all-errors --retry-delay 2 --fail \
  http://127.0.0.1:18080/api/public/experiments/enterobacteria-identification
docker compose -f compose.dev.yaml down -v
```

如果中途失败：

```bash
docker compose -f compose.dev.yaml logs --no-color
docker compose -f compose.dev.yaml down -v
```

## 7. 多架构正式发布

推荐只通过 `.github/workflows/release.yml` 发布：

1. `main` CI 全绿；
2. 创建 annotated `v*` tag；
3. Release 工作流再次运行完整 CI；
4. QEMU/Buildx 构建并推送 amd64/arm64；
5. 发布不可变版本、major.minor、major、sha 标签；
6. 稳定版更新 `latest`；
7. 创建 GitHub Release 和 SHA256SUMS。

手工多架构推送仅用于紧急修复，并需要具有 `write:packages` 权限的 GHCR 凭据：

```bash
VERSION="v1.1.1"
echo "$GHCR_TOKEN" | docker login ghcr.io -u CodeAIX --password-stdin

docker buildx build \
  --platform linux/amd64,linux/arm64 \
  --file infra/docker/Dockerfile.app \
  --tag "ghcr.io/codeaix/microbio-lab-app:${VERSION}" \
  --push .

docker buildx build \
  --platform linux/amd64,linux/arm64 \
  --file infra/docker/Dockerfile.builder \
  --tag "ghcr.io/codeaix/microbio-lab-builder:${VERSION}" \
  --push .

docker logout ghcr.io
unset GHCR_TOKEN
```

禁止把 GHCR Token 写入脚本、`.env`、shell history、Actions 日志或仓库 Secret 以外的位置。

## 8. 发布后验证

```bash
docker buildx imagetools inspect ghcr.io/codeaix/microbio-lab-app:v1.1.1
docker buildx imagetools inspect ghcr.io/codeaix/microbio-lab-builder:v1.1.1
```

必须确认：

- manifest 同时包含 `linux/amd64` 和 `linux/arm64`；
- 两个 Package 为 Public；
- 未登录 Docker 客户端可以拉取；
- GitHub Release 不是 draft 或 prerelease；
- Release 资产 SHA256 校验通过。

匿名拉取测试：

```bash
docker logout ghcr.io 2>/dev/null || true
docker pull ghcr.io/codeaix/microbio-lab-app:v1.1.1
docker pull ghcr.io/codeaix/microbio-lab-builder:v1.1.1
```

## 9. 制作离线 Docker archive

以下命令在有 Docker 的联网机器上制作适用于 x86_64 VPS 的完整镜像集合：

```bash
TARGET_PLATFORM="linux/amd64"
VERSION="v1.1.1"
mkdir -p 最新版镜像

docker pull --platform "$TARGET_PLATFORM" "ghcr.io/codeaix/microbio-lab-app:${VERSION}"
docker pull --platform "$TARGET_PLATFORM" "ghcr.io/codeaix/microbio-lab-builder:${VERSION}"
docker pull --platform "$TARGET_PLATFORM" postgres:17-alpine
docker pull --platform "$TARGET_PLATFORM" nginx:1.27-alpine

docker image save -o "最新版镜像/microbio-lab-app-${VERSION}-linux-amd64.tar" \
  "ghcr.io/codeaix/microbio-lab-app:${VERSION}"
docker image save -o "最新版镜像/microbio-lab-builder-${VERSION}-linux-amd64.tar" \
  "ghcr.io/codeaix/microbio-lab-builder:${VERSION}"
docker image save -o "最新版镜像/postgres-17-alpine-linux-amd64.tar" postgres:17-alpine
docker image save -o "最新版镜像/nginx-1.27-alpine-linux-amd64.tar" nginx:1.27-alpine

(cd 最新版镜像 && sha256sum *.tar > SHA256SUMS)
```

在 ARM64 机器上把平台和文件名改为 `linux/arm64`。不要把两个架构混在同名 Docker archive 中。

离线 VPS 导入：

```bash
cd 最新版镜像
sha256sum -c SHA256SUMS
for IMAGE_ARCHIVE in *.tar; do docker load -i "$IMAGE_ARCHIVE"; done
docker image ls | grep -E 'microbio-lab|postgres|nginx'
```

## 10. 本地交付包结构

标准离线交付包：

```text
MicroBioLab-v1.1.1-offline-linux-amd64/
├── 构建方案.md
├── HANDOFF.md
├── README-OFFLINE.md
├── MANIFEST.txt
├── SHA256SUMS
├── 镜像源码/                 # 从 v1.1.1 tag 导出的完整源码
├── 部署文件/                 # compose、env 示例、infra、scripts、运维说明
└── 最新版镜像/
    ├── microbio-lab-app-v1.1.1-linux-amd64.tar
    ├── microbio-lab-builder-v1.1.1-linux-amd64.tar
    ├── postgres-17-alpine-linux-amd64.tar
    ├── nginx-1.27-alpine-linux-amd64.tar
    └── SHA256SUMS
```

外层推荐使用 Zstandard：

```bash
tar -cf - MicroBioLab-v1.1.1-offline-linux-amd64 \
  | zstd -T0 -19 -o MicroBioLab-v1.1.1-offline-linux-amd64.tar.zst
sha256sum MicroBioLab-v1.1.1-offline-linux-amd64.tar.zst \
  > MicroBioLab-v1.1.1-offline-linux-amd64.tar.zst.sha256
```

## 11. 可复现性与限制

- npm 依赖由 `package-lock.json` 固定；
- 正式镜像 tag 不可覆盖，但 Dockerfile 基础镜像目前使用上游标签而非 digest；
- 完全逐字节可复现需要进一步把 `node:24-bookworm-slim` 固定到 OCI digest；
- PostgreSQL/Nginx 的浮动标签也应在离线包 `MANIFEST.txt` 中记录实际 digest；
- 离线包不包含生产 `.env`、数据库数据、管理员密码或 Session Secret；
- 生产数据迁移使用 `scripts/backup.sh`，不能用源码/镜像包代替备份。
