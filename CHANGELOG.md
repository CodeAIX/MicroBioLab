# Changelog

## v1.5.1

- 知识点复习中的 8 列及以上宽表在桌面端自动适配内容区宽度并换行；普通表格保持原样，小屏幕继续支持横向滚动。

## v1.5.0

- 修复批量更新 JSX 弹窗中目录选择控件与提示文字错位的问题。
- 实验列表时间列简化为“最新版更新”，每个实验只显示当前生效版本的发布时间。
- 压缩实验管理详情页的版本卡片布局，集中状态与操作区，并提高表单、版本字段和按钮字号。

## v1.4.0

- 实验列表新增目录批量更新 JSX：递归读取子目录，以 `<slug>-vsim.jsx` 或 `<slug>.jsx` 匹配现有模块，并在上传前展示 SHA256 预检结果。
- 新增持久化批次与构建进度；只有全部版本构建成功且当前版本未变化时，才允许在单个数据库事务中整体发布，避免半批次上线。
- 新增安全批量清理：仅允许删除非当前、从未发布且不在构建中的版本，并展示源码与构建产物预计占用；发布历史继续保留用于回滚。
- 实验列表、批次和版本历史增加精确日期时间，便于区分同日更新。
- `mbl` 维护命令新增 `storage` 菜单与参数入口，用于查看在线资产和本机备份占用。
- 集成与 Playwright 回归测试覆盖批量预检、整体发布和安全清理流程。

## v1.3.0

- 新增 `mbl` 中文交互式维护命令，统一提供状态、健康检查、日志、备份、启停、重启、标准升级、回滚、恢复和分级卸载入口。
- 全新安装和后续升级会自动在 `/usr/local/bin/mbl` 注册维护命令；参数模式可用于非交互运维。
- 新增 VPS 引导式部署脚本：下载固定版本 Release 并校验 SHA256，生成随机密钥和环境配置，启动服务、检查健康状态并创建首位管理员。

## v1.2.0

- 为每个实验增加独立的 Markdown 知识点复习资源，新建实验时可选上传，管理端可随时替换或删除。
- 学生实验介绍页在存在知识点时显示“知识点复习”按钮，并以支持表格、代码块和折叠答案的悬浮阅读页按需加载内容。
- 知识点原文保存在数据库中，限制为 UTF-8 `.md`、最大 512 KiB；渲染前执行 HTML 白名单清洗。
- 清理初始 V1 实施规格、停止维护的离线包说明和已不适用的宿主机 Nginx 示例。

## v1.1.2

- 将单个 JSX 文件上传上限从 2 MiB 提高到 10 MiB，并统一浏览器、multipart 入口和服务端二次校验。
- 封面图片继续保持 2 MiB 独立上限，避免 multipart 入口放宽后意外接受超大封面。
- 本地 v1.1.1 离线包作为历史备份保留，不再随在线版本更新。

## v1.1.1

- 完善在线、离线镜像安装和通用 VPS 运维说明。
- 健康检查增加就绪重试，避免升级或回滚后过早误报失败。
- 完整备份新增历史构建产物，恢复前校验并清理对应旧资产。
- 升级与回滚先备份、预拉取目标镜像，并同步维护 App/Builder 版本标识。
- 卸载支持独立清理平台镜像，继续默认保留配置和永久数据。
- 新增 `HANDOFF.md`、`构建方案.md`，并升级 GitHub Actions 到当前受支持主版本。

## v1.1.0

- 管理端可自定义教学顺序，学生首页按保存后的顺序展示已发布实验。
- 新增实验介绍页、稳定正式实验地址和手机二维码；发布新版后链接与二维码保持不变。
- 优化学生首页封面与简介可读性，并为未上传封面的实验生成稳定主题色块。
- 管理端可编辑实验基本信息、替换或删除封面、删除未发布版本。
- 已归档实验可在输入完整 Slug 并二次确认后永久删除数据库记录和全部关联文件。
- 新增数据库增量迁移和覆盖以上功能的集成、浏览器回归测试。

## v1.0.4

- 将超出 2 MiB 的封面上传映射为明确的 `UPLOAD_TOO_LARGE` 错误。

## v1.0.3

- 修复异步上传完成后访问已卸载表单导致的 `Cannot read properties of null (reading 'reset')`。

## v1.0.2

- 允许同源管理后台 iframe 加载受认证保护的构建预览，同时保持其他主站响应禁止嵌入。
- 为管理员预览增加浏览器端到端回归测试。

## v1.0.1

- 修复受限用户运行的实验静态服务无法写入 Nginx tmpfs 缓存目录的问题。
- 开发集成栈使用与生产一致的只读文件系统、权限和健康检查，防止该问题回归。

## v1.0.0

- 完整管理员认证、实验资产与不可变版本管理。
- 隔离式 JSX 验证、构建、预览、发布与回滚。
- 公共学生入口、独立实验 Origin 与 iframe sandbox。
- Docker Compose、VPS 运维脚本、CI、GHCR 多架构发布。
